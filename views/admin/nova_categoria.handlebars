{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}    

{{/each}}


<h3>Nova Categoria:</h1>
<form method="POST" action="/admin/categorias/add" class="form-control">
    <div class="mb-3 mt-3">
        <label for="nome" class="form-label">Nome:</label>
        <input type="text" class="form-control" id="nome" placeholder="Digite o Nome da Categoria" name="nome">
    </div>
    <div class="mb-3">
        <label for="slug" class="form-label">Slug:</label>
        <input type="text" class="form-control" id="slug" placeholder="Digite o Slug da Categoria" name="slug">
    </div>
    <button type="submit" class="btn btn-success">Salvar</button>
</form>
